specVersion: "v1"
serviceId: "qatratkheir"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "qatratkheir-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
